<%= form_for(@memory_slot) do |f| %>
  <% if @memory_slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@memory_slot.errors.count, "error") %> prohibited this memory_slot from being saved:</h2>

      <ul>
      <% @memory_slot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
