<%= form_for(@form_factor) do |f| %>
  <% if @form_factor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@form_factor.errors.count, "error") %> prohibited this form_factor from being saved:</h2>

      <ul>
      <% @form_factor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :string %><br>
    <%= f.text_field :string %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
